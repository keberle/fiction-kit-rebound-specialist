% PDF Dark Mode LaTeX Header
% Used by compile-episode.ps1 for dark mode PDF generation

% Required for colors
\usepackage{xcolor}

% Define dark mode colors
\definecolor{darkbg}{HTML}{1a1a1a}
\definecolor{lighttext}{HTML}{e0e0e0}

% Set page background to dark using xcolor's pagecolor
\pagecolor{darkbg}

% Set default text color to light
\color{lighttext}

% Increase spacing between paragraphs for better readability
\setlength{\parskip}{0.75em}
